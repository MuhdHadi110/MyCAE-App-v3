# ============================================================
# Production Environment Configuration
# ============================================================
# Copy this file to .env and update with actual values
# NEVER commit the actual .env file to version control
# ============================================================

# ----------------------
# REQUIRED VARIABLES
# ----------------------
# The application will fail to start without these

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=<your_db_username>
DB_PASSWORD=<your_secure_db_password>
DB_NAME=<your_db_name>

# Server Configuration
NODE_ENV=production
PORT=3000

# CORS Configuration (comma-separated list of allowed origins)
# Example: https://app.yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=https://yourdomain.com

# JWT Configuration
# IMPORTANT: Generate a secure random string (min 32 characters)
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=<generate-64-char-hex-string>

# ----------------------
# RECOMMENDED VARIABLES
# ----------------------
# Application will warn if these are missing

# Email/SMTP Configuration (for password resets, notifications)
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@yourdomain.com
SMTP_PASSWORD=<your_smtp_password>
SMTP_FROM="MyCAE Tracker <noreply@yourdomain.com>"

# reCAPTCHA Configuration (for bot protection on login)
# Get keys from: https://www.google.com/recaptcha/admin
RECAPTCHA_SECRET_KEY=<your_recaptcha_secret_key>

# ----------------------
# OPTIONAL VARIABLES
# ----------------------

# Frontend URL (for email links)
FRONTEND_URL=https://yourdomain.com

# n8n Webhook Integration (for automation)
N8N_WEBHOOK_URL=

# ----------------------
# SECURITY CHECKLIST
# ----------------------
# Before deploying to production:
# [ ] Generate new JWT_SECRET (never reuse development secrets)
# [ ] Use strong DB_PASSWORD (min 16 chars, mixed case, numbers, symbols)
# [ ] Configure real reCAPTCHA keys (not test keys)
# [ ] Set up HTTPS/SSL certificate
# [ ] Verify CORS_ORIGINS matches your production domain
# [ ] Configure email service for password resets
# [ ] Set up database backups
# [ ] Review rate limiting settings
