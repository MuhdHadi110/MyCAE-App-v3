# ============================================
# MyCAE Tracker - PRODUCTION Config for cPanel
# ============================================
# INSTRUCTIONS:
# 1. Copy this file to your cPanel backend folder as ".env"
# 2. Fill in all values marked with <REPLACE_THIS>
# 3. Save and restart your Node.js app in cPanel

# Database Configuration
# Get these from cPanel > MySQL Databases after creating your database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=<REPLACE_WITH_YOUR_DB_NAME>        # Example: cpanelusername_mycae
DB_USER=<REPLACE_WITH_YOUR_DB_USER>        # Example: cpanelusername_mycae_user
DB_PASSWORD=<REPLACE_WITH_YOUR_DB_PASSWORD>

# JWT Configuration
# Generate a secure random string (64+ characters)
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=<REPLACE_WITH_LONG_RANDOM_STRING>
JWT_EXPIRES_IN=7d

# Google reCAPTCHA v2 (optional - can leave empty if not using)
RECAPTCHA_SECRET_KEY=

# Server Configuration
PORT=3001
NODE_ENV=production

# IMPORTANT: Set this to your actual domain
FRONTEND_URL=https://yourdomain.com

# CORS Configuration
# IMPORTANT: Add your actual domain(s) here
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Exchange Rates API - Using Frankfurter (Free, no API key required)
# No configuration needed

# Email Configuration (OPTIONAL - Configure if you need email features)
# Get SMTP details from your hosting provider or email service
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=true
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=noreply@yourdomain.com

# n8n Workflow Integration (OPTIONAL)
N8N_WEBHOOK_URL=
N8N_API_KEY=
N8N_WORKFLOW_NEW_CHECKOUT=
N8N_WORKFLOW_RETURN_DUE=
N8N_WORKFLOW_LOW_STOCK=
N8N_WORKFLOW_MAINTENANCE_TICKET=
N8N_WORKFLOW_PROJECT_ASSIGNED=

# Backup Configuration (OPTIONAL)
ONEDRIVE_BACKUP_PATH=

# ============================================
# SECURITY CHECKLIST:
# ============================================
# ✓ Change JWT_SECRET to a unique random string
# ✓ Use strong database password
# ✓ Never commit this file to version control
# ✓ Set correct CORS_ORIGINS with your domain
# ✓ Enable HTTPS (SSL) in cPanel
# ✓ Keep this file permissions to 600 (read/write owner only)
